# 추가 설계 필요사항

## 🔍 문서 검토 결과

현재 설계 문서들을 검토한 결과, 다음 항목들이 추가 설계가 필요합니다.

---

## 1. 🔐 Canvas API 인증 방식 구체화 (중요)

### 현재 상태
- "OAuth 2.0 또는 API Token"이라고만 명시
- 구체적인 구현 방법 부재

### 문제점
Canvas API는 일반적으로:
- ❌ OAuth 2.0 Client Credentials Grant를 지원하지 않음
- ✅ Personal Access Token (PAT) 사용
- ✅ LTI Advantage Access Token 사용 (더 적합)

### 해결 방안

**옵션 A: LTI Advantage Access Token 사용 (추천)**

```ruby
# app/services/lti/canvas_api_token_generator.rb
module Lti
  class CanvasApiTokenGenerator
    # LTI Advantage Access Token 획득
    # Canvas가 제공하는 OAuth 2.0 Token Endpoint 사용
    def self.generate(lti_claims, canvas_url, client_id, client_secret)
      # LTI Claims에서 deployment_id 추출
      deployment_id = lti_claims[:deployment_id]
      
      # Canvas OAuth 2.0 Token Endpoint
      token_url = "#{canvas_url}/login/oauth2/token"
      
      # JWT Assertion 생성
      assertion = generate_jwt_assertion(client_id, canvas_url, deployment_id)
      
      # Access Token 요청
      response = HTTParty.post(
        token_url,
        body: {
          grant_type: "client_credentials",
          client_assertion_type: "urn:ietf:params:oauth:client-assertion-type:jwt-bearer",
          client_assertion: assertion,
          scope: "https://canvas.instructure.com/lti/public_jwk/scope/update"
        }
      )
      
      response.parsed_response["access_token"]
    end
    
    private
    
    def self.generate_jwt_assertion(client_id, canvas_url, deployment_id)
      payload = {
        iss: client_id,
        sub: client_id,
        aud: "#{canvas_url}/login/oauth2/token",
        exp: Time.now.to_i + 3600,
        iat: Time.now.to_i,
        jti: SecureRandom.uuid
      }
      
      # JWT 서명 (LTI Developer Key의 Private Key 사용)
      JWT.encode(payload, private_key, 'RS256')
    end
  end
end
```

**옵션 B: Canvas Personal Access Token (대안)**

```ruby
# Canvas 사용자가 생성한 Personal Access Token 사용
# 환경변수로 관리하거나 DB에 저장
# 단점: 사용자별로 토큰 관리 필요
```

**추가 고려사항**:
- Canvas API Token의 유효기간 관리
- Token 갱신 로직
- 여러 Canvas 인스턴스별 Token 관리

---

## 2. 📦 세션 관리 전략

### 현재 상태
- "세션에 LTI Claims 저장"이라고만 명시
- 세션 만료 시 처리 방법 부재

### 설계 필요

```ruby
# app/controllers/concerns/lti_session_manager.rb
module LtiSessionManager
  extend ActiveSupport::Concern
  
  included do
    before_action :load_lti_claims_from_session
  end
  
  private
  
  def load_lti_claims_from_session
    @lti_claims = session[:lti_claims]
    
    # 세션 만료 시 LTI Launch 재요청 필요
    unless @lti_claims
      redirect_to lti_login_path, alert: "세션이 만료되었습니다."
    end
  end
  
  def save_lti_claims_to_session(lti_claims)
    session[:lti_claims] = lti_claims
    session[:lti_claims_expires_at] = 1.hour.from_now
  end
end
```

**고려사항**:
- 세션 만료 시간 설정
- 세션 만료 시 사용자 경험 (재로그인 vs 에러 메시지)
- Redis 세션 스토어 사용 여부

---

## 3. 👤 Canvas User ID 매핑 전략

### 현재 상태
- "LTI User Sub를 Canvas User ID로 매핑 필요"라고만 명시
- 구체적인 매핑 방법 부재

### 문제점
Canvas API 호출 시 Canvas User ID가 필요한 경우:
- Submission 생성 시 `user_id` 필요
- Assignment 조회 시 사용자별 권한 확인

### 해결 방안

**옵션 A: LTI Claims의 canvas_user_id 사용 (가장 간단)**

```ruby
# LTI Launch 시 받은 canvas_user_id 사용
canvas_user_id = @lti_claims[:canvas_user_id]

# 단점: 모든 Canvas 인스턴스에서 제공하지 않을 수 있음
```

**옵션 B: Canvas API로 사용자 조회**

```ruby
# app/services/lti/user_mapper.rb
module Lti
  class UserMapper
    # Canvas API로 사용자 조회
    def self.get_canvas_user_id(lti_user_sub, canvas_url, access_token)
      # Canvas API: GET /api/v1/users/:id (LTI User Sub로 조회)
      # 또는 Canvas API: GET /api/v1/courses/:course_id/users (코스 사용자 목록)
      # LTI User Sub와 매칭
    end
  end
end
```

**옵션 C: 매핑 테이블 생성**

```ruby
# db/migrate/xxx_create_lti_user_mappings.rb
create_table :lti_user_mappings do |t|
  t.string :lti_user_sub, null: false
  t.integer :canvas_user_id, null: false
  t.string :platform_iss, null: false
  t.string :canvas_url, null: false
  t.timestamps
end
add_index :lti_user_mappings, [:lti_user_sub, :platform_iss], unique: true
```

**추천**: 옵션 A (canvas_user_id) → 옵션 B (API 조회) → 옵션 C (매핑 테이블)

---

## 4. 🚨 에러 처리 전략

### 현재 상태
- 에러 처리 방법이 구체적이지 않음

### 설계 필요

```ruby
# app/services/canvas_api/client.rb
module CanvasApi
  class Client
    class ApiError < StandardError
      attr_reader :status, :response
      
      def initialize(status, response)
        @status = status
        @response = response
        super("Canvas API Error: #{status} - #{response}")
      end
    end
    
    def create_assignment(course_id, params)
      response = self.class.post(...)
      
      case response.code
      when 200..299
        response.parsed_response
      when 401
        raise ApiError.new(401, response), "인증 실패. Canvas API Token을 확인하세요."
      when 403
        raise ApiError.new(403, response), "권한이 없습니다."
      when 404
        raise ApiError.new(404, response), "코스 또는 리소스를 찾을 수 없습니다."
      when 422
        errors = response.parsed_response["errors"] || []
        raise ApiError.new(422, response), "유효성 검사 실패: #{errors.join(', ')}"
      else
        raise ApiError.new(response.code, response), "Canvas API 호출 실패"
      end
    rescue HTTParty::Error => e
      Rails.logger.error "Canvas API 네트워크 에러: #{e.message}"
      raise ApiError.new(500, nil), "Canvas API 연결 실패"
    end
  end
end

# app/controllers/concerns/canvas_api_error_handler.rb
module CanvasApiErrorHandler
  extend ActiveSupport::Concern
  
  included do
    rescue_from CanvasApi::Client::ApiError, with: :handle_canvas_api_error
  end
  
  private
  
  def handle_canvas_api_error(exception)
    Rails.logger.error "Canvas API Error: #{exception.message}"
    
    case exception.status
    when 401
      redirect_to projects_path, alert: "Canvas 인증에 실패했습니다. 다시 시도해주세요."
    when 403
      redirect_to projects_path, alert: "이 작업을 수행할 권한이 없습니다."
    when 404
      redirect_to projects_path, alert: "요청한 리소스를 찾을 수 없습니다."
    else
      redirect_to projects_path, alert: "오류가 발생했습니다: #{exception.message}"
    end
  end
end
```

---

## 5. 🔄 Assignment 업데이트/삭제 시나리오

### 현재 상태
- Assignment 생성만 설계됨
- 업데이트/삭제 시나리오 부재

### 설계 필요

```ruby
# app/services/project_builder.rb
class ProjectBuilder
  def update_project(project_params)
    # 기존 Assignment 업데이트
    existing_assignment_ids = project_params[:assignments]&.select { |a| a[:id].present? }&.map { |a| a[:id] }
    
    # 삭제할 Assignment (Project에 있지만 params에 없는 경우)
    assignment_ids_to_delete = @project.assignment_ids - existing_assignment_ids
    
    assignment_ids_to_delete.each do |assignment_id|
      @canvas_api.delete_assignment(@lti_context.context_id, assignment_id)
    end
    
    # 새 Assignment 생성
    new_assignments = project_params[:assignments]&.select { |a| a[:id].blank? }
    new_assignment_ids = new_assignments.map do |assignment_params|
      response = @canvas_api.create_assignment(@lti_context.context_id, assignment_params)
      response['id']
    end
    
    # 기존 Assignment 업데이트
    existing_assignment_ids.each do |assignment_id|
      assignment_params = project_params[:assignments].find { |a| a[:id] == assignment_id }
      @canvas_api.update_assignment(@lti_context.context_id, assignment_id, assignment_params)
    end
    
    # Project 업데이트
    @project.assignment_ids = existing_assignment_ids + new_assignment_ids
    @project.save!
  end
end
```

---

## 6. 📊 Submission 조회 및 통계

### 현재 상태
- Submission 통계 조회 방법이 구체적이지 않음

### 설계 필요

```ruby
# app/services/canvas_api/submissions_client.rb
module CanvasApi
  class SubmissionsClient < Client
    # Assignment의 모든 Submission 조회
    def get_submissions(course_id, assignment_id, params = {})
      self.class.get(
        "/api/v1/courses/#{course_id}/assignments/#{assignment_id}/submissions",
        query: params
      )
    end
    
    # 특정 사용자의 Submission 조회
    def get_user_submission(course_id, assignment_id, user_id)
      self.class.get(
        "/api/v1/courses/#{course_id}/assignments/#{assignment_id}/submissions/#{user_id}"
      )
    end
    
    # Submission 통계
    def get_submission_summary(course_id, assignment_id)
      self.class.get(
        "/api/v1/courses/#{course_id}/assignments/#{assignment_id}/submission_summary"
      )
    end
  end
end

# app/services/project_service.rb
class ProjectService
  def assignments_with_submission_statistics(project)
    project.assignment_ids.map do |assignment_id|
      assignment = @canvas_api.get_assignment(@lti_context.context_id, assignment_id)
      summary = @canvas_api.get_submission_summary(@lti_context.context_id, assignment_id)
      
      {
        id: assignment_id,
        title: assignment['name'],
        submitted_count: summary['submitted_count'],
        unsubmitted_count: summary['unsubmitted_count'],
        graded_count: summary['graded_count']
      }
    end
  end
end
```

---

## 7. 💾 캐싱 전략

### 현재 상태
- 캐싱 전략이 명시되지 않음

### 설계 필요

```ruby
# app/services/canvas_api/client.rb
module CanvasApi
  class Client
    # Assignment 조회 캐싱 (5분)
    def get_assignment(course_id, assignment_id)
      cache_key = "canvas:assignment:#{course_id}:#{assignment_id}"
      
      Rails.cache.fetch(cache_key, expires_in: 5.minutes) do
        response = self.class.get(...)
        response.parsed_response
      end
    end
    
    # Submission 통계 캐싱 (1분)
    def get_submission_summary(course_id, assignment_id)
      cache_key = "canvas:submission_summary:#{course_id}:#{assignment_id}"
      
      Rails.cache.fetch(cache_key, expires_in: 1.minute) do
        response = self.class.get(...)
        response.parsed_response
      end
    end
    
    # Assignment 생성/수정/삭제 시 캐시 무효화
    def create_assignment(course_id, params)
      response = self.class.post(...)
      assignment_id = response.parsed_response['id']
      
      # 관련 캐시 무효화
      Rails.cache.delete("canvas:assignments:#{course_id}")
      
      response.parsed_response
    end
  end
end
```

---

## 8. ⚙️ 환경 설정

### 현재 상태
- 환경별 설정이 명시되지 않음

### 설계 필요

```ruby
# config/initializers/canvas_api.rb
module CanvasApi
  class Config
    def self.canvas_url
      ENV['CANVAS_URL'] || raise("CANVAS_URL environment variable is required")
    end
    
    def self.api_timeout
      ENV.fetch('CANVAS_API_TIMEOUT', 30).to_i
    end
    
    def self.retry_attempts
      ENV.fetch('CANVAS_API_RETRY_ATTEMPTS', 3).to_i
    end
    
    def self.cache_enabled
      ENV.fetch('CANVAS_API_CACHE_ENABLED', 'true') == 'true'
    end
  end
end

# config/environments/development.rb
config.canvas_api = {
  timeout: 30,
  retry_attempts: 3,
  cache_enabled: true
}

# config/environments/production.rb
config.canvas_api = {
  timeout: 10,
  retry_attempts: 2,
  cache_enabled: true
}
```

---

## 9. 🔒 보안 고려사항

### 현재 상태
- 보안 고려사항이 구체적이지 않음

### 설계 필요

```ruby
# app/controllers/concerns/lti_security.rb
module LtiSecurity
  extend ActiveSupport::Concern
  
  included do
    # CSRF 보호 (LTI는 이미 CSRF skip)
    # 하지만 일반 컨트롤러에서는 필요
    protect_from_forgery with: :exception, except: [:lti_launch]
    
    # XSS 방지
    before_action :sanitize_params
  end
  
  private
  
  def sanitize_params
    # 사용자 입력값 sanitize
    params.each do |key, value|
      if value.is_a?(String)
        params[key] = ActionController::Base.helpers.sanitize(value)
      end
    end
  end
end

# Canvas API Token 보안
# - 환경변수로 관리
# - 절대 코드에 하드코딩하지 않음
# - Token 만료 시간 관리
# - Token 갱신 로직
```

---

## 10. 📈 성능 최적화

### 현재 상태
- 성능 최적화 전략 부재

### 설계 필요

```ruby
# N+1 쿼리 방지
# Project 목록 조회 시 Assignment ID 배열을 한 번에 조회
def fetch_assignments_batch(assignment_ids)
  # Canvas API Batch 요청 (가능한 경우)
  # 또는 병렬 요청
  assignment_ids.map do |assignment_id|
    Thread.new do
      @canvas_api.get_assignment(@lti_context.context_id, assignment_id)
    end
  end.map(&:value)
end

# Rate Limiting 대응
# Canvas API Rate Limit: 600 requests per 10 minutes
class CanvasApi::RateLimiter
  def self.check_rate_limit
    # Redis로 요청 횟수 추적
    # Rate Limit 초과 시 대기 또는 에러
  end
end
```

---

## 11. 🧪 테스트 전략

### 현재 상태
- 테스트 전략이 명시되지 않음

### 설계 필요

```ruby
# spec/services/canvas_api/client_spec.rb
RSpec.describe CanvasApi::Client do
  let(:client) { described_class.new(canvas_url, access_token) }
  
  describe '#create_assignment' do
    it 'Canvas API로 Assignment 생성' do
      # VCR 또는 WebMock 사용
      # Canvas API 응답 모킹
    end
  end
end

# spec/controllers/projects_controller_spec.rb
RSpec.describe ProjectsController do
  describe 'LTI 인증' do
    it 'LTI Launch 없이 접근 시 에러' do
      # 세션에 LTI Claims가 없는 경우
    end
  end
end
```

---

## 12. 📝 로깅 및 모니터링

### 현재 상태
- 로깅 전략이 명시되지 않음

### 설계 필요

```ruby
# app/services/canvas_api/client.rb
def create_assignment(course_id, params)
  Rails.logger.info "Canvas API 호출 시작", {
    action: 'create_assignment',
    course_id: course_id,
    timestamp: Time.now
  }
  
  response = self.class.post(...)
  
  Rails.logger.info "Canvas API 호출 성공", {
    action: 'create_assignment',
    course_id: course_id,
    assignment_id: response.parsed_response['id'],
    timestamp: Time.now
  }
  
  response.parsed_response
rescue => e
  Rails.logger.error "Canvas API 호출 실패", {
    action: 'create_assignment',
    course_id: course_id,
    error: e.message,
    timestamp: Time.now
  }
  raise
end
```

---

## ✅ 우선순위별 정리

### 🔴 높은 우선순위 (구현 필수)
1. **Canvas API 인증 방식 구체화** - 가장 중요
2. **세션 관리 전략** - 사용자 경험에 직접 영향
3. **에러 처리 전략** - 안정성 확보
4. **Canvas User ID 매핑** - Submission 생성 시 필요

### 🟡 중간 우선순위 (구현 권장)
5. **Assignment 업데이트/삭제** - 완전한 CRUD 구현
6. **Submission 조회 및 통계** - 기능 완성도
7. **캐싱 전략** - 성능 최적화

### 🟢 낮은 우선순위 (선택사항)
8. **환경 설정** - 운영 편의성
9. **보안 고려사항** - 기본 보안은 이미 구현됨
10. **성능 최적화** - 필요 시 추가
11. **테스트 전략** - 품질 보증
12. **로깅 및 모니터링** - 운영 편의성

---

## 📝 다음 단계

1. **Canvas API 인증 방식 결정** (옵션 A vs B)
2. **세션 관리 구현**
3. **에러 처리 구현**
4. **나머지는 구현하면서 추가**

---

**작성일**: 2025-01-06  
**작성자**: AI Assistant

